{
  "name": "gisspy",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf ./dist/* && npx tsc && bun build src/index.ts --minify --outdir dist"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "next": "^14.2.3",
    "node-mocks-http": "^1.14.1"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}